var websiteFormAttribute = {
	header : 'website-form',
	minLength : 0,
	maxLength : 9999999,
	isnull : true
}

window.websiteFormValidate = function(){
	$('website-form-check').each(function(i , v){
		
	});
}
